# Python >= 3.10 required (developed with 3.11.9)
# ================================================
# Core RAG Dependencies
# ================================================

# Vector Database (pure Python, no Docker needed)
chromadb>=0.5.0

# HTTP client for Ollama embedding API
requests>=2.31.0

# LLM unified interface (OpenAI-compatible)
openai>=1.30.0

# PDF text extraction
pypdf>=4.0.0

# Environment variable management
python-dotenv>=1.0.0
